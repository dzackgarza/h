/build
coverage
node_modules
yarn-error.log
.coverage*
.tox
*.egg-info
*.pyc
supervisord.log
supervisord.pid
.DS_Store
.devdata*
.eslintcache
*.tsbuildinfo
.vscode
.hypothesis/

# Pyramid debug mailer
mail/

# development TLS cert/key
.tlscert.pem
.tlscsr.pem
.tlskey.pem

# See https://yarnpkg.com/getting-started/qa#which-files-should-be-gitignored.
# This is the "If you're not using Zero-Installs" list.
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions
data/
pre-volume-migration.sql

# Bundled Node KaTeX normalizer deps (reproduce with `pnpm install` in that dir)
h/scripts/html-normalize/node_modules/

# Celery beat runtime schedule (dbm shelf) -- regenerated, never source
celerybeat-schedule

# Images the Mathpix emulator had no recording for. Transient: bin/record_mathpix_fixtures.py
# turns them into fixtures and removes them.
tests/corpus/mathpix/unrecorded/

# mutmut runs from a copy of the project; it is regenerated, never committed.
mutants/
