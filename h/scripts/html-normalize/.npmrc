# A hoisted node_modules is a real directory tree rather than a farm of symlinks into
# the pnpm store, so it survives being copied -- which is what mutation testing does to
# the project on every run. Without this the copied tree fails with ERR_MODULE_NOT_FOUND.
node-linker=hoisted
